name: Word Diff
on:
  pull_request:
  push:
    branches:
      - main
jobs:
  diff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: SuffolkLITLab/ALActions/word_diff@main
